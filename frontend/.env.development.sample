# Development environment configuration

# API URL for local development worker
# Used for all API requests from the frontend
REACT_APP_API_URL=http://localhost:8787

# Public URL for development environment
# Used for serving static assets and configuring base URL
REACT_APP_PUBLIC_URL=http://localhost:3000

# Authorized email domain for authentication
# Only users with emails from this domain can access the application
REACT_APP_AUTH_DOMAIN=example-dev.com

